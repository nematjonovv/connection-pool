{
  "name": "connection-pool",
  "version": "1.0.0",
  "description": "Thread-safe connection pool for network connections with automatic reuse",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/index.ts",
    "example": "ts-node examples/postgres.ts",
    "test": "ts-node test/pool.test.ts"
  },
  "dependencies": {
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.41",
    "@types/pg": "^8.10.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
